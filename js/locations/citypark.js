// citypark.js
// Placeholder for the City Park location.
// Replace this render() function with the real page content and
// activity logic once it's built out, using the original
// scripts/citypark/*.pl files as a spec for expected behavior.

KidsTown.locations["citypark"] = {
  render(container) {
    container.innerHTML = `
      <section class="location">
        <h1>City Park</h1>
        <p>Coming soon!</p>
      </section>
    `;
  },
};
