// library.js
// Placeholder for the Library location.
// Replace this render() function with the real page content and
// activity logic once it's built out, using the original
// scripts/library/*.pl files as a spec for expected behavior.

KidsTown.locations["library"] = {
  render(container) {
    container.innerHTML = `
      <section class="location">
        <h1>Library</h1>
        <p>Coming soon!</p>
      </section>
    `;
  },
};
