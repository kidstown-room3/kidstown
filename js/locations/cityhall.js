// cityhall.js
// Placeholder for the City Hall location.
// Replace this render() function with the real page content and
// activity logic once it's built out, using the original
// scripts/cityhall/*.pl files as a spec for expected behavior.

KidsTown.locations["cityhall"] = {
  render(container) {
    container.innerHTML = `
      <section class="location">
        <h1>City Hall</h1>
        <p>Coming soon!</p>
      </section>
    `;
  },
};
