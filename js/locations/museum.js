// museum.js
// Placeholder for the Museum location.
// Replace this render() function with the real page content and
// activity logic once it's built out, using the original
// scripts/museum/*.pl files as a spec for expected behavior.

KidsTown.locations["museum"] = {
  render(container) {
    container.innerHTML = `
      <section class="location">
        <h1>Museum</h1>
        <p>Coming soon!</p>
      </section>
    `;
  },
};
